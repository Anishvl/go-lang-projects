package main
import(
  "fmt"
)
func main(){
  var a,b int
  fmt.Println("enter two numbers to swap")
  fmt.Scanln(&a)
  fmt.Scanln(&b)
  fmt.Println("The values before Swaping is",a,b)
  var temp int
  temp = a
  a=b
  b=a
  fmt.Println("The values before Swaping is",a,b)
  temp=_
}